# Contributing

Open an issue before large changes. Keep adapters thin and place shared solving behavior in capsolver-agent or capsolver-core. Add tests and documentation for every behavior change.

Never commit API keys, cookies, solved tokens, proxy credentials, private URLs, personal data, or live target-site fixtures. Run the repository CI-equivalent checks before opening a pull request.
